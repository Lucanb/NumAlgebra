function H = cholesky(A)

[m,n] = size(A);
h = zeros(n);
h(1,1) = sqrt(A(1,1));
for i = 2:n
    for j = 1:i-1
        sum = 0;
        for k = 1:j-1
           sum = sum + h(i,k)*h(j,k)
        h(i,j) = (a(i,j) -  sum)
    end

end